# Ambient music beds

Music is synthesized on-the-fly by the GitHub Actions runner using ffmpeg
sine/aevalsrc/aecho filters (see scripts/autopilot-runner.mjs → buildMusicBed).
Zero external dependency, zero Content ID risk, deterministic per video.
